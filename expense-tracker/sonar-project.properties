# SonarQube / SonarCloud Configuration

sonar.projectKey=expense-tracker
sonar.organization=your-github-org
sonar.projectName=Expense Tracker
sonar.projectVersion=1.0

# Paths to source directories
sonar.sources=backend/app,frontend/src
sonar.tests=backend/tests
# sonar.tests=frontend/tests

# Language specific rules
sonar.python.version=3.11
sonar.javascript.environments=browser,react

# Exclusions
sonar.exclusions=**/node_modules/**,**/venv/**,**/__pycache__/**,**/*.test.js,**/*.test.jsx,**/dist/**

# Coverage reporting paths (if configured in CI)
# sonar.python.coverage.reportPaths=backend/coverage.xml
# sonar.javascript.lcov.reportPaths=frontend/coverage/lcov.info
